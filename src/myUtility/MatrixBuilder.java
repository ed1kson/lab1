package myUtility;
import java.lang.reflect.*;

public class MatrixBuilder {
    public static Object[][] createMatrix(int rows, int columns, Type type) {
        if ( rows <= 0 || columns <= 0 ) return null;
        if ( rows == 1 || columns == 1 ) {
            
        }
        return null;
    } 
}
